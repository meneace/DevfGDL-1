let ana = ["murcielago"];
let vsana = ["ogaleicrup"];
let anab = [];

function isAnagram(ana, vsana) {
    var ana = str1.toLowerCase();
    var vsana = str2.toLowerCase();
}
console.log(str1);
console.log(str2);
var isAnagram = str1.length > 0 && str1.length === str2.length && (str1.toLowerCase().split('').sort().join('') === str2.toLowerCase().split('').sort().join(''));

console.log(isAnagram);